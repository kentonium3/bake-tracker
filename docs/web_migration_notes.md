# Web Rebuild Notes — MOVED

This document has moved to the `bake-planner` repository, which is its canonical home.

**Canonical location**: `bake-planner/docs/architecture/decisions.md`

This stub is retained so that references to this path in `bake-tracker` (e.g., Constitution
Principle VIII) do not break. Do not update this file — update the canonical version instead.
